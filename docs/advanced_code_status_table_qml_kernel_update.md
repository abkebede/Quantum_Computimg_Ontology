# Advanced Code Status Table Update — Quantum Machine Learning Kernel

Add the following row to the master advanced code status table:

```markdown
| Quantum Machine Learning Kernel | `12_advanced_codes/qml/quantum_kernel_intro.py` | Working example | Tested — runs successfully | Advanced | Quantum machine learning / kernels / pattern recognition | Extend to a full kernel matrix and simple classification workflow |
```

## Short status note

The QML kernel example passed. It produced a feature-map circuit and two kernel values:

```text
K(x,y): 0.9946124562671147
K(x,z): 0.09767350810296573
```

The output correctly illustrates that larger kernel values indicate more similar quantum feature states.
