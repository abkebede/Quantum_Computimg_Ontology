# Advanced Code Status Table Update — Simulator Noise Study Template

Add the following row to the advanced code status table.

```markdown
| Simulator noise study template | `12_advanced_codes/hardware_noise/simulator_noise_study_template.py` | Workflow/template example | Tested — runs successfully | Advanced | Hardware noise / simulator-vs-hardware realism | Add numerical ideal-vs-noisy counts and error-fraction calculation in a future version |
```

## Notes

This file ran successfully and printed a structured workflow for comparing ideal simulator results with noisy simulator or hardware results.

It should remain classified as a **template/workflow** until a future version includes a concrete noisy-simulator calculation with printed counts.
