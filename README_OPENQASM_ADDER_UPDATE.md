# OpenQASM Cuccaro Ripple-Carry Adder Teaching Unit Update

This is a partial update for the existing `qc-code-study-library` repository. It is not a replacement repository and does not rename the project folder.

This update adds a teaching unit for:

```text
06_openqasm_examples/cuccaro_ripple_carry_adder.qasm
```

The unit explains the OpenQASM 3 syntax, majority/unmajority gates, reversible arithmetic, binary addition, and the role of carry propagation in quantum circuits.

Suggested commit message:

```text
Add OpenQASM ripple-carry adder teaching unit and metadata
```
